"""Research specialist sub-agent — external web + attendee profiles.

Owned tools: analyzeAttendeeBackground, searchWebInfo. Returns compact
structured summaries to the orchestrator; never writes the final brief.
"""
