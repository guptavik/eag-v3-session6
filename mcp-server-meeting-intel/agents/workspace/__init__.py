"""Workspace specialist sub-agent — Google Calendar + Gmail.

Owned tools: getUpcomingMeetings, searchGmail. Returns compact
structured summaries to the orchestrator; never writes the final brief.
"""
